{{- if .Values.zitadel.bundled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "stackweaver.fullname" . }}-login-ui
  labels:
    {{- include "stackweaver.labels" . | nindent 4 }}
    app.kubernetes.io/component: login-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "stackweaver.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: login-ui
  template:
    metadata:
      labels:
        {{- include "stackweaver.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: login-ui
    spec:
      {{- include "stackweaver.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: login-ui
          image: "{{ .Values.zitadel.loginUI.image.repository }}:{{ .Values.zitadel.loginUI.image.tag }}"
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: NEXT_PUBLIC_BASE_PATH
              value: /ui/v2/login
            - name: ZITADEL_API_URL
              value: "http://{{ include "stackweaver.fullname" . }}-zitadel:8080"
            - name: ZITADEL_SERVICE_USER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "stackweaver.secrets.zitadel" . }}
                  key: {{ .Values.secrets.zitadel.keys.loginServiceUserToken }}
                  optional: true
            - name: EMAIL_VERIFICATION
              value: "false"
            # x-zitadel-instance-host: tells Zitadel which instance to serve (by ExternalDomain).
            # x-forwarded-host: overrides the publicHostname Zitadel derives from the incoming
            # connection. Without this, Kubernetes health-check probes hit the pod at its pod IP
            # (e.g. 100.64.0.43:3000), the login-ui forwards that as x-forwarded-host, and
            # Zitadel rejects it because the pod IP is not a trusted domain.
            # Docker Compose: network_mode: host means probes use localhost (trusted); no override needed.
            - name: CUSTOM_REQUEST_HEADERS
              value: "x-zitadel-instance-host:{{ .Values.ingress.hosts.auth }},x-forwarded-host:{{ .Values.ingress.hosts.auth }}"
          livenessProbe:
            httpGet:
              path: /ui/v2/login
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /ui/v2/login
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            {{- toYaml .Values.zitadel.loginUI.resources | nindent 12 }}
{{- end }}
